---
description: Social Archiver 项目全局约定
alwaysApply: true
---

# Social Archiver 全局约定

- 产品名 **Social Archiver / 社交存档**；禁止恢复旧名 Media Harvester
- 开发说明真源：`交接文档.md`；启动命令见 `README.md`；产品边界 `PRODUCT.md`；界面 `DESIGN.md`
- 栈：Electron + Vue（`src/`）+ Python 后端（`backend/`）；打包内含冻结 `backend.exe` + gallery-dl
- 平台：微博、X、Instagram；只缓存**原创**（不含转发/转推）；增量写入同一 `{root}/{platform}/{user_id}/`
- 开发启动：`npm.cmd run electron:dev`（PowerShell 用 `npm.cmd`）；改 `electron-main.js`/`preload.js` 需重启
- 自检：`npm.cmd test`（Vitest + Python unittest）、`npm.cmd run build`
- 改功能后同步 README / 交接文档 / PRODUCT / DESIGN（版本号一致，当前见交接文档）
- 仅在我明确要求时 git commit；不自动 push
