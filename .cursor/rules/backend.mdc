---
description: Python 后端缓存、Cookie 与增量逻辑
globs: backend/**,tests/**
alwaysApply: false
---

# Python 后端

- 入口：`backend/cli.py`；平台：`weibo.py`、`twitter.py`、`instagram.py`
- 输出结构：`{platform}/{user_id}/`，含 `_posts/`、媒体、`index.html`、`_profile.json`、头像 `_avatar.*`
- 增量：微博连续 N 条已缓存原创可停翻页；IG/X 用 gallery-dl download archive；勿整页重拉
- Cookie：X 需 `auth_token`+`ct0`；微博 `m.weibo.cn` SUB；IG `sessionid`；guest token 不够
- stdout 发 JSON 行须行缓冲，避免 `done` 被拆成两半导致 UI 一直转圈
- 校验：`backend/verify.py`；Cookie 预检：`backend/cookie_check.py`
- 无头批量：`backend.exe --batch-config batch.json`（示例 `batch.example.json`）
- 便携：`portable.flag` 与 exe 同目录时配置跟目录走，默认存档根 `archives/`
- 改后端逻辑补 `tests/test_*.py`；大日期范围回归注意 `backend/daterange.py`
