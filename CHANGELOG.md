# Changelog

用户向变更记录。开发细节见 [交接文档.md](交接文档.md)。

版本号真源：`package.json` 的 `version`。发 zip 前见 [RELEASE.md](RELEASE.md)。

## 1.2.4（2026-09-04）

### 修复

- 微博用户名若不是数字 UID（含抽不出 ID 的自定义域名主页），会拦住不开始缓存，避免建成错误目录
- 浏览侧栏按钮改用与缓存/设置页相同的 `sa-btn`，去掉旧的独立皮肤

### 文档

- 当前说明与代码对齐：Edge 导入不覆盖 `per_user`、Instagram 分区只在缓存为空时回填、浏览卡为分栏布局

## 1.2.3（2026-09-04）

### 使用体验

- 缓存用户名可粘贴主页链接（`x.com/xxx`、`instagram.com/xxx`、`weibo.com/u/数字`），自动抽出 ID
- 已保存的 Cookie 默认折叠为「已登录」，需要时再展开重新登录或从 Edge 导入
- 浏览侧栏「添加用户」与缓存页共用同一套用户名 / 登录组件，并预填该平台已保存的 Cookie
- 浏览顶栏拆成主操作与筛选两行；切到缓存/设置再回来仍停留在当前用户，不再强制回概览

## 1.2.2（2026-09-01）

### 修复

- 切换任务时不再因为远端 Cookie 校验抖动而反复弹登录窗
- Instagram 应用内登录分区 Cookie 不再盲目覆盖已缓存的 per_user `sessionid`
- Edge 导入不再覆盖同平台下其它用户的 per_user Cookie（之前会把同平台所有账号的缓存都改成 Edge 当前账号的 Cookie）

### 缓存与登录

- 新增进程内 30 分钟 Cookie 验证 TTL：缓存任务完成后或 `checkCookie` 确认有效后，30 分钟内切换任务免远端校验
- 浏览页点「更新」先看 TTL，命中直接跑；X / Instagram / 微博都生效
- 设置页 / 缓存页 Cookie 校验通过后自动写入 TTL；`refresh-instagram-session` 成功后同样标记
- 进程内 Cookie 校验存储使用对象条目（避免原字符串 key 在 userId 含特殊字符时切分错位）

## 1.2.1（2026-08-31）

### 修复

- X 缓存完一个用户再换另一个时，沿用已刷新的 `ct0`，避免 gallery-dl 仍用过期登录态失败
- gallery-dl 失败时显示真实退出码与错误行，不再一律写成「退出码未知」
- Instagram 离线 `index.html` 多图轮播不再导出为 `[object Object]`

### 浏览

- Instagram 帖子改为桌面式分栏卡（左媒体 / 右配文）；轮播支持箭头与圆点（应用内与离线 HTML 一致）

### 缓存与登录

- 设置页 / 缓存页支持从 Edge 导入 Cookie 并自动校验
- Edge 导入会同步更新该平台下已有按用户保存的 Cookie，避免仍读取旧 `per_user` 键
- IG / X 用户名输入自动规范化（去 `@`、空格转 `_`），含非法字符时阻止开始缓存

## 1.2.0（2026-08-31）

### 浏览（Phase 7）

- 用户时间线侧边栏：**月历导航**，按发帖日跳转筛选
- 顶栏 **#话题筛选**，从帖子正文提取标签

### 缓存

- 批量更新失败时 **指数退避自动重试**（设置页可关 / 调次数）

### 分发与更新

- 设置页：**当前版本** + **检查更新**（可选 `update_manifest_url`）
- 新增 [SIGNING.md](SIGNING.md) 代码签名说明（可选）

### 维护与修复

- 生产环境注入 Content-Security-Policy；开发模式抑制已知 CSP 警告
- `electron:dev` 启动前自动释放 5173 端口，避免连到旧 Vite 进程白屏
- 修复话题筛选模块 ESM 导出导致浏览页白屏

## 1.1.1（2026-08-31）

### 浏览

- 默认进入「存档概览」：纯文字统计与用户列表，不自动显示帖子缩略图
- 点选用户后才加载时间线；可「返回概览」
- 切到缓存/设置再回浏览，也会回到概览
- 搜索、设置页校验「查看帖子」仍直达对应帖子

### 界面

- 缓存页：左表单、右日志双栏，全屏利用宽度；日志在面板内滚动
- 设置页：左常规配置、右维护与 Cookie
- 分组面板、按钮/输入框交互反馈、深色层次微调

### 维护

- 版本号统一到 `package.json`（1.1.1）
- 发版前可跑 `npm run release:check` / `npm run release:pack`

## 1.1.0（2026-08-29）

- 浏览页日期筛选、搜索高亮、JSON 导出
- 设置页校验向导、微博实况修复、更新历史与代理设置

## 1.0.0 及更早

见 [交接文档.md](交接文档.md) 版本历史。
