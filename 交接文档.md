# 开发说明

Social Archiver（社交存档）。Windows 桌面应用。位置 `D:\AI\work\social-archiver`。更新于 2026-08-22。

先看 [README.md](README.md) 里的启动命令。产品边界 [PRODUCT.md](PRODUCT.md)，视觉 [DESIGN.md](DESIGN.md)。旧名 Media Harvester 不要写回界面。

---

## 已测（2026-08-17）

本机真实存档跑过，不要再当未知项：

1. **微博** `C:\Users\terence\Downloads\6622994776`：增量缓存、时间线排序、实况一格（悬停 / 收起 / 查看大图）、普通视频窗口内播放。Chromium 解不了的（常见 HEVC 实况）会出「打开实况」或「用系统播放器打开」，文件夹里单独打开能播。
2. **X** 真实账号（如 AMD）：原创文字/图片/视频/GIF 缓存、时间线、头像。GIF 是 mp4，用控件播，不是 LIVE。头像走 `https://x.com/{handle}/photo`，落成 `_avatar.png` 或 `_avatar.jpg`。
3. **Instagram** 真实账号：原创图片/视频/轮播缓存、时间线、头像。通过 gallery-dl 1.32.5 内置 Instagram extractor 下载，需 `sessionid` Cookie。

浏览：**刷新**只扫盘；**更新**才联网。点更新后才会补新头像。

**0.4.1（2026-08-22）**：修复 Instagram Cookie 误存 twitter 键（`session.js` 按平台分键，浏览页「更新」不再拿 X 的 Cookie 跑 Instagram）；新增 Instagram 浅色皮肤（`skin.js` / `style.css` / `BrowseView` / `PostCard`）；删除 `HomeView` 未导入的 `path` 引用，恢复 `vue-tsc` / 打包。已过类型检查和 vite 构建；**真实存档回归未跑**，首跑注意 IG 浏览皮肤和「更新」取 Cookie 是否正常。

---

## 打开开发版

```bat
cd /d D:\AI\work\social-archiver
npm.cmd run electron:dev
```

第一次先 `npm.cmd install` 和 `pip install -r backend\requirements.txt`。PowerShell 用 `npm.cmd`。

| 改了什么 | 要不要重启窗口 |
|----------|----------------|
| `electron-main.js` / `preload.js` | 要 |
| Vue 前端 | 否（热更新） |
| `backend/*.py` | 否（下次点缓存就用新代码） |

打包：`npm.cmd run electron:build`（写图标 → `vue-tsc` + vite → `python scripts/build_backend.py` → electron-builder zip）。

产物：

- `dist-electron/SocialArchiver-0.4.0-win.zip` — 解压即用
- `dist-electron/win-unpacked/` — 同内容，本机可直接跑 `Social Archiver.exe`

PyInstaller onedir 输出到 `build-backend/backend/`，electron-builder 拷进 `resources/backend/`。**不要只拷贝 exe**，必须保留整个 `win-unpacked` 目录。对方不用装 Python。打包依赖：`pip install -r backend/requirements-build.txt`。若 `win-unpacked` 被占用（旧 exe 还在跑），先关掉再打包；也可 `--config.directories.output=%TEMP%\sa-electron-out` 打到临时目录再拷回。

打包版与开发版共用同一套 JSON Lines 协议；`backend/cli.py` 的 `_emit()` 直接写 UTF-8 字节到 stdout，`electron-main.js` 用 `Buffer.toString('utf8')` 读，`resolveBackendPath()` 还会设 `PYTHONIOENCODING=utf-8` / `PYTHONUTF8=1`。

---

## 怎么工作

Electron 34 + Vue 3。微博自己请求 `m.weibo.cn`；X 和 Instagram 调 gallery-dl 1.32.5。主进程 spawn Python / 冻结 `backend.exe`，stdout 一行一条 JSON（UTF-8）。

```
electron-main.js / preload.js   spawn backend；stdout UTF-8 JSON Lines
backend/          cli.py（_force_utf8_stdio / _emit）→ downloader.py → weibo.py | twitter.py | instagram.py
                  daterange.py  metadata.py  migrate_archives.py（旧平铺档迁平台子目录）
scripts/          build_backend.py（PyInstaller）  write_icon.py
src/views/        HomeView 缓存 · BrowseView 浏览 · SettingsView
src/components/   PostCard（微博 LIVE 悬停 / X 控件播视频 / Instagram 浅色卡片）
src/utils/        livePhoto.js  archiveHtml.js  postTime.js  session.js（Cookie 按平台分键）
```

存档在用户选的根目录 `{platform}/{user_id}/`（platform 为 weibo / twitter / instagram）：

- `_posts/{日期}/{id}.json`
- `{日期}/` 媒体文件
- `_profile.json`
- `_avatar.jpg` / `_avatar.png` / `_avatar.webp` / `_avatar.gif`（浏览会按这个顺序找）
- `index.html`（缓存成功会重写；浏览页也可再生成）
- X 另有 `.download-archive.sqlite`、`.twitter-cookies.txt`
- Instagram 无额外缓存文件

旧版平铺在 `{root}/{user_id}/` 的存档：浏览页仍可直接读；要归位到平台子目录，跑 `python backend/migrate_archives.py <根目录>`（预览），确认后加 `--apply` 实际移动。

Cookie：微博要 `m.weibo.cn` 的 `SUB`（不要 weibo.com 桌面站 Cookie）。X 要 `auth_token` + `ct0`。Instagram 要 `sessionid`。

排序是时间戳，不是星期几字符串。微博实况图一格（静图 + 悬停）；X 和 Instagram 没有 LIVE 格。

本地文件协议：`social-archiver://asset/?path=`。`.mov` 按 `video/mp4`。≤100MB 的文件用整文件 Buffer 做 HTTP Range（`206`），`<video>` 才能拖进度。必须在 `app.whenReady` 之前 `registerSchemesAsPrivileged`（含 `standard` + `stream`）。播不了时 `shell.openPath`（preload：`openLocalPath`）。

不要用 `Readable.toWeb(fs.createReadStream)` 给 `<video>` 喂流，Range 会断。

---

## 微博

- API：`https://m.weibo.cn/api/container/getIndex`，`containerid=107603{uid}`。
- 翻页：`type=uid`、`value`、`since_id`。置顶博文不要当成「已到起始日期」。
- 图片：不要把 API 的 `Host` / Cookie 带到 `wx*.sinaimg.cn`。Referer `https://weibo.com/`，走 `/large/`，wx1–4 重试。
- 头像：用户对象里的 `profile_image_url` / `avatar_hd`，写成 `_avatar.jpg`。
- 实况图下载必须走 play 接口，**不要** GET 裸的 `livephoto.us.sinaimg.cn`（403）：

```
GET https://video.weibo.com/media/play?livephoto=https%3A%2F%2Flivephoto.us.sinaimg.cn%2Fxxx.mov
Referer: https://weibo.com/
不要 Cookie，不要自定义 Host
```

返回 H.264 QuickTime。文件小于 64 字节或开头是 `{` `<` `[` 都是假文件，要重下（旧档里有 43 字节 JSON 冒充 mp4）。

相关代码：`backend/weibo.py`（`_livephoto_play_url`、`_media_file_ok`）、`src/utils/livePhoto.js`、`PostCard.vue`、`electron-main.js`。

悬停仍失败时：对应 `_N.mov` 是否有 `ftyp`；协议响应是不是 `video/mp4`；若是 HEVC（`hvc1`），窗口里解不了，应出现「打开实况」。

---

## X

- `backend/twitter.py` → 冻结后的 `backend.exe --run-gallery-dl` 或开发态 `python -m gallery_dl` → `https://x.com/{handle}/tweets`。纯数字 ID 用 `https://x.com/id:{id}/tweets`。不要用 `/media`。不要再依赖 PATH 上的 `gallery-dl` 命令。
- 只要原创 + 纯文字帖。`retweets/quoted/replies` 关。`size: orig`，不要视频封面、卡片、文章附件，丢掉 m3u8。视频取 `video_info.variants` 里 bitrate 最高的 mp4。
- 日期用 `date-after` / `date-before`（1.32.5 忽略 `date-min`/`date-max`）。边界在 `backend/daterange.py`。
- 没有微博那种实况图。GIF 也是 mp4，浏览里用控件播，不要做成 LIVE 悬停。
- 头像：推文拉完再跑一次 `https://x.com/{handle}/photo`（数字 ID 用 `id:{id}/photo`），文件名 `_avatar.{extension}`，写到用户目录根。失败或超时（45 秒）不打断整次缓存，浏览页先显示首字母。不要对 `pbs.twimg.com` 另开无 Cookie 的 `requests.get`（容易超时，而且和 gallery-dl 会话不一致）。
- 整理元数据时跳过 `_` / `.` 开头的文件，避免把 `_profile.json` 当成帖子。

相关代码：`backend/twitter.py`（`_fetch_twitter_avatar`、`_normalize_twitter_archive`）、`backend/metadata.py`（`existing_avatar`）、`electron-main.js`（`findAvatar`）。

---

## Instagram

- 通过 gallery-dl 1.32.5 内置 Instagram extractor 下载。只认 `sessionid` Cookie，不支持用户名密码登录。
- 用户 ID 可以是用户名（如 `username`）或数字 ID（如 `id:12345`）。
- 下载图片、视频、轮播（carousel），按 gallery-dl 的 `{media_id}.{extension}` 命名，存到 `{user_id}/{YYYY-MM-DD}/`。
- 日期范围通过 `date-after` / `date-before` 控制。
- 头像：单独跑一次 gallery-dl avatar extractor，文件名 `_avatar.{extension}`，超时 45 秒不打断主流程。
- 私密账号：需登录用户已关注目标，否则会报错。
- 请求间隔：gallery-dl 内置 6-12 秒随机延迟，避免被限流。
- Cookie 存储按平台分键（`settings.json` 的 `cookies.twitter` / `cookies.weibo` / `cookies.instagram`），浏览页「更新」按存档平台取对应 Cookie（`src/utils/session.js` 的 `platformCookieKey`）。2026-08-22 修复过 Instagram Cookie 误存 twitter 键的 bug；旧档若 IG Cookie 存进过 twitter 键，需重新粘贴一次。
- 浏览皮肤三套：微博浅色、X 深色、Instagram 浅色（IG 蓝 `#0095f6`，`skin.js` / `style.css` / `BrowseView` / `PostCard` 都认 `instagram`）。IG 帖子用浅色卡片（wb-post 分支），话题/提及按单 `#`、`@handle` 语法高亮。

相关代码：`backend/instagram.py`（`download_instagram_media`、`_normalize_instagram_archive`）、`backend/metadata.py`（`download_avatar`）、`electron-main.js`（`instagram-login`）。

---

## 已知问题

- 安装包是 zip，不是 NSIS。解压后的 `Social Archiver.exe` 通过 `resources/backend/backend.exe` 跑缓存；X 和 Instagram 在冻结进程里用 `--run-gallery-dl` 调 gallery-dl，不要再找 PATH 上的 `gallery-dl`。未签名 exe 可能 SmartScreen 拦截。
- 打包版缓存日志曾按 GBK 输出导致界面乱码；已在 `cli.py` + `electron-main.js` 固定 UTF-8（2026-08-16）。若仍乱码，确认用的是新 zip / 新 `win-unpacked`，不是旧目录里单独拷出来的 exe。
- 微博应用内登录部分机器会闪退；改在系统浏览器打开 `m.weibo.cn` 后粘贴 Cookie。日志：`%APPDATA%\social-archiver\weibo-login-debug.log`。
- 启动关 GPU / WebGL，避免旧崩溃。因此窗口里解不了 HEVC；这是预期，不是下载失败。
- Instagram 需 `sessionid` Cookie，且只支持公开账号或登录用户已关注的私密账号。sessionid 过期后需重新登录。
- Instagram 请求间隔为 6-12 秒，大账号缓存较慢，属正常限流保护。

未做（用户没点名就别做）：去 Element、ffmpeg 转封装。

---

## 配置

`%APPDATA%\social-archiver\settings.json`：输出目录、并发、命名模板、上次平台。Cookie 只留本机。
