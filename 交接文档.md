# 开发说明

Social Archiver（社交存档）。Windows 桌面应用。更新于 **2026-09-06**（v1.2.5）。

先看 [README.md](README.md) 里的启动命令。产品边界 [PRODUCT.md](PRODUCT.md)，视觉 [DESIGN.md](DESIGN.md)，拓展路线 [拓展计划.md](拓展计划.md)。旧名 Media Harvester 不要写回界面。

**版本号真源**：`package.json` 的 `version`（当前 **1.2.5**）。`electron-builder` 产物名、Git tag、发版说明与之保持一致；README / 交接文档写产物路径时用 `<version>` 占位，不要写死旧号。**发 zip 流程见 [RELEASE.md](RELEASE.md)**（`npm.cmd run release:pack`）。改功能后请同步 README / 交接文档 / CHANGELOG；PRODUCT / DESIGN 按界面或边界变更再改。

---

## 版本历史

### v1.2.5（2026-09-06）— 下载可靠性

- **状态收尾**：CLI 检测到 `error` 事件时以失败状态退出；主进程要求后端返回 `done`，避免界面或批量队列将半截任务误判完成
- **等待可见**：下载 30 秒没有输出时，主进程向单次/批量日志发心跳，提示可能正在限流或重试
- **微博续传**：`_download_one` 写 `{媒体}.part`；有效临时文件以 `Range: bytes=N-` 续传，服务端忽略 Range 时重新下载；校验通过后 `os.replace` 为正式文件
- **未知总量进度**：gallery-dl 与微博进度事件标为 `total_known: false`，前端显示动画条与「已完成 N 个」，不再伪造整体百分比
- **批量恢复**：`pending-batch.json` 保存排队与当前任务；窗口完成加载后自动恢复。正常完成或用户停止会清理快照
- **原子 JSON**：`backend/metadata.py` 的帖子/资料写入与 `electron/settings-store.js` 的设置写入均为临时文件 + flush/fsync + replace
- **测试**：新增微博 Range 续传/回退、元数据原子写入测试；当前 `npm test` 为 87 个 Vitest + 84 个 Python unittest

### v1.2.4（2026-09-04）— 遗留核对

- **文档对齐**：当前操作说明改为 Edge 导入只写平台全局键、IG 分区仅在缓存为空时回填、IG 浏览卡分栏布局（版本历史里的旧描述保留）
- **微博 UID**：非数字 ID（含 `weibo.com/n/…` 自定义域名）拦截，避免把 URL 当成目录名
- **侧栏按钮**：`UserManager` 改用 `sa-btn` / `sa-btn-ghost` / `sa-btn-danger`

### v1.2.3（2026-09-04）— 日常操作减负

- **主页链接**：`normalizeUserId` 从 X / Instagram / 微博主页 URL 抽出 handle 或 UID
- **Cookie 折叠**：已有可用 Cookie 时登录区显示「已登录」；「重新登录」才展开 F12 / 应用内登录
- **侧栏添加用户**：复用 `UserIdInput` + `CookieInput`，打开表单时预填该平台已保存 Cookie
- **浏览位置**：切 tab 不再重置到存档概览；顶栏拆成主操作行与筛选行

### v1.2.2（2026-09-01）— 切换任务不再重登

- **Cookie 验证 TTL**：进程内 30 分钟 TTL。缓存任务完成或 `checkCookie` 确认有效后，30 分钟内切换任务直接信任，不再打 `x.com` / `instagram.com` / `m.weibo.cn` 远端
- **浏览页短路**：`BrowseView.ensureCookie` 先问 `isCookieRecentlyValidated`，命中就跳过 `checkCookie`，避免网络抖动时反复弹登录窗
- **IG 分区不再盲覆盖**：`resolveInstagramJobCookie` 重写为「缓存可信则用缓存；缓存为空才回填分区」，避免切到其它账号时把 per_user `sessionid` 误覆盖
- **Edge 导入不再误伤**：`applyBrowserCookieImports` 只更新 `cookies[platform]`，per_user 各用户 Cookie 保留；导入后清掉该平台的 TTL 记录，下次任务重新校验
- **新模块**：`electron/cookie-validation.js` 进程内 Cookie 验证存储（对象条目，避免字符串 key 在 userId 含特殊字符时切分错位）；新增 IPC `mark-cookie-valid` / `is-cookie-recently-validated`
- **测试**：`tests/electron-cookie-validation.test.js`（10 用例）+ `tests/electron-cookie-sources.test.js` 重写 `applyBrowserCookieImports` 断言（不再期望 per_user 被覆盖）

### v1.2.1（2026-08-31）— X Cookie 与浏览抛光

- **X 会话 Cookie**：gallery-dl 刷新后的 `ct0` 写到 `{root}/twitter/.session-cookies.txt`；同一 `auth_token` 换用户沿用，并写回设置 / 缓存页
- **批量队列**：每个任务开始前再读一次最新平台 Cookie，避免入队时的过期快照
- **错误信息**：gallery-dl 失败带真实退出码与错误行
- **Edge 导入**：设置页 / 缓存页「从 Edge 导入」；同步更新该平台下已有 `per_user` Cookie，避免仍读旧按用户键
- **Instagram 浏览卡**：应用内分栏布局（左媒体 / 右配文）；离线 `index.html` 同布局，轮播含箭头与圆点
- **用户名输入**：IG / X 自动规范化（去 `@`、空格转 `_`），缓存前校验非法字符
- **离线 HTML**：修复 Instagram 多图轮播导出为 `[object Object]` 的问题

### v1.2.0（2026-08-31）— Phase 7 阅读延伸 + 分发更新

- **月历导航**：浏览用户时间线侧边栏按 `_posts/{date}/` 跳转
- **话题筛选**：顶栏 `#标签` 从正文提取并过滤
- **批量失败退避**：批量缓存失败自动重试（设置可关，默认最多 3 次）
- **检查更新**：设置页版本号 + 可选 `update_manifest_url`（见 `scripts/update-manifest.example.json`）
- **代码签名说明**：[SIGNING.md](SIGNING.md)
- **CSP 安全**：`electron/security.js` 为打包版注入 CSP；开发模式抑制 Vite HMR 的已知警告
- **开发体验**：`scripts/electron-dev.ps1` 启动前释放 5173；修复话题筛选 ESM 导出白屏

### v1.1.1（2026-08-31）— 浏览隐私入口与界面抛光

- **存档概览**：浏览页默认显示纯文字统计与用户列表，不自动加载帖子缩略图
- **返回概览**：用户时间线顶栏可回到概览；切到缓存/设置再回浏览也会回到概览
- **深链接保留**：设置页校验问题「查看帖子」、搜索命中仍直达对应帖子
- **Chrome 抛光**：统一 `sa-section` 分组面板、输入框 focus/hover、按钮过渡；深色模式层次微调；缓存/设置页分组更清晰
- **缓存页日志坞**：表单区可滚动，底部固定高度日志面板（内部滚动），长任务不再撑高整页
- **全宽双栏布局**：缓存页左表单右日志；设置页左常规配置、右维护与 Cookie（全屏利用宽度）
- **发版流程**：`scripts/release-check.ps1`、`release-pack.ps1`；`npm run release:check` / `release:pack`；[CHANGELOG.md](CHANGELOG.md)、[RELEASE.md](RELEASE.md)
- **修复**：`normalize_concurrent(0)` 应钳制为 1（`gallery_dl_runner.py`）
- **主进程拆分**：`electron/`（IPC 分模块）+ `electron-archives.js`；`electron-main.js` 瘦身为入口
- **存档 fixture 测试**：`tests/fixtures/archive/` + `tests/electron-archives.test.js`（scan → get-posts 链路）

### v1.1.0（2026-08-29）— Phase 6 阅读与运维深化

- **日期范围筛选**：浏览顶栏按发帖日过滤时间线 / 画廊
- **搜索高亮**：结果 snippet 与打开帖子后正文标黄
- **JSON 全量导出**：`export-{user}-{date}.json`
- **校验修复向导**：设置页问题列表可「查看帖子」「更新用户」
- **微博实况修复**：删除损坏 mp4，更新用户可重拉
- **更新历史**：`{存档根}/update-log.jsonl`；可选桌面通知
- **网络代理**：设置页 `proxy_url`；仅 X / Instagram 缓存与登录使用；微博直连
- **暂缓（用户确认）**：微博评论区配图、X 回复串（对话树）暂不实施；X **回复时间线**（用户自己发的回复帖）已在 v0.8.0 完成。详见 [拓展计划.md](拓展计划.md)「暂缓功能」。

### v1.0.0（2026-08-29）— Phase 5 生态与分发

- **Obsidian 导出增强**：`export-md/{year}/{date}/{id}.md`；front matter 含 `title`、`kind`、`tags`（含 `#话题`）
- **本地 RSS**：浏览页「导出 RSS」→ `{userDir}/feed.xml`，供阅读器订阅本机存档
- **X 书签 / 点赞**（可选）：独立目录 `twitter/{user}--bookmarks`、`twitter/{user}--likes`；`kind: bookmark|like` 标签
- **SmartScreen 说明**：README 补充未签名安装包处理方式
- **跨平台构建评估**：见下文「macOS / Linux 冒烟」；正式产物仍为 Windows

### v0.9.0（2026-08-29）— Phase 4 平台与架构

- **`gallery_dl_runner.py`**：Twitter / Instagram 共用 gallery-dl 下载循环、配置写入、日期过滤
- **平台注册表**：`backend/platforms/registry.py` + `src/platforms/registry.ts`；`downloader.py` 经注册表分发
- **PostCard 拆分**：`src/components/posts/`（`FeedStylePost`、`XPost`、`usePostCard`）；主 `PostCard.vue` 仅路由
- **NSIS 安装器**：`npm run electron:build` 额外产出 `.exe` 安装包；zip 仍保留
- **跨用户时间线**：浏览页可选「全部用户（只读）」；`get-all-posts` IPC 合并排序

### v0.8.0（2026-08-29）— Phase 3 内容与媒体

- **X 回复时间线**（可选）：`/with_replies`，`kind: reply`，浏览/缓存页开关
- **X / 微博引用帖**（可选）：`kind: quote`，标注「引用自 @xxx」
- **置顶帖**：X `pinned` + 微博置顶；浏览排序置顶优先，紫色标签
- **ffmpeg 转码**：HEVC→H.264，设置页检测与一键转码；`{原名}_h264.mp4`
- **视频封面**：ffmpeg 截取首帧，生成 `{原名}_poster.jpg`；卡片 `<video poster>` 减少黑屏
- **Instagram 轮播**：gallery-dl 元数据 + 子目录分组；`carousel_index` / `carousel_total`
- **Instagram Reels / Stories**（可选）：缓存页与用户管理分开关；Stories 提示 24h 时效；`kind: reel` / `story` 标签

### v0.7.0（2026-08-29）— Phase 2 效率与导出

- **便携模式**：exe 同目录放 `portable.flag`，配置与 `data/` 跟目录走，默认存档根 `archives/`
- **Markdown 导出**：浏览页导出到 `{userDir}/export-md/`，Obsidian 可直接打开
- **媒体画廊**：浏览页「时间线 / 画廊」切换，点击缩略图回到帖子
- **浏览统计**：资料卡显示帖数、媒体数、磁盘占用、日期范围
- **每用户更新策略**：用户管理侧栏可选手动 / 每日 / 每周
- **定时批量更新**：设置页启用，到点自动更新设为每日/每周的用户（应用需保持运行）
- **无头 CLI**：`backend.exe --batch-config batch.json`；示例见 `batch.example.json`

### v0.6.0（2026-08-29）— Phase 1 可用性底座

- **微博深度回溯**：缓存页 / 用户管理可勾选；`_profile.json` 记录 `lastSinceId`、`lastPage`、`fetchStatus`；断点续拉
- **fetchStatus**：`complete` / `partial` / `page_limit`；完成时界面提示是否还有更早内容
- **浏览页搜索**：存档根 `_index/search-cache.json` 索引；顶栏搜索框跨用户检索
- **帖子深链接**：`#post-{id}`，应用内与离线 `index.html` 均支持
- **完整性校验**：`backend/verify.py`，设置页「校验全部存档」
- **Cookie 预检**：`backend/cookie_check.py`，设置页「检测全部 Cookie」
- **浏览器 Cookie 导入**：`backend/browser_cookies.py` + gallery-dl `load_cookies`；设置页与缓存页「从 Edge 导入」并自动校验；读取前须完全退出 Edge
- **Instagram 分区刷新**：缓存 IG 前，仅当设置里 `sessionid` 缺失或与 `persist:instagram-login` 分区不一致时才写回设置；缓存页可手动「刷新应用内登录」

### v0.5.4（2026-08-28）

阅读主题：

- 浏览/缓存/设置统一为浅色或深色两套主题（默认浅色），顶栏切换；偏好键 `sa-browse-theme`
- 移除三平台配色（微博橙 / X 蓝 / IG 蓝），`skin.js` 改为 `theme.js`，帖子布局仍按平台区分
- 离线 `index.html` 使用同一套 token，导出时带入当前主题，页内可切换深浅色
- `DESIGN.md` 设计方向从 Local Mirror 调整为 Local Reader

### v0.5.3（2026-08-28）

品牌与打包：

- 应用图标换成新设计：深色圆角底 + 等距三层叠板（橙 / 天蓝 / 品红），象征分层归档；源图去白底后铺满画布，无外侧白框
- 源图 `icons/social-archiver-icon-1024.png`（及 512 版）；备选稿在 `icons/previews/`；`scripts/write_icon.py` 去 AI 白边并生成多尺寸 `icon.ico`（依赖 Pillow）
- 打包产物文件名随版本升为 `SocialArchiver-0.5.3-win.zip`

### v0.5.2（2026-08-28）

性能与日常摩擦（全部完成）：

- 微博增量：连续 5 条已缓存且媒体完整的原创（非置顶）就停翻页；打满 200 页会明确提示，不再看起来像全部成功
- Instagram 补上 `.download-archive.sqlite`，和大号增量不再整条时间线重走
- 浏览时间线懒加载帖子卡片；图片/视频 URL 在渲染进程直接拼 `social-archiver://`，不再每张图一次 IPC
- `scan-archives` 直接带 `lastUpdate`；`get-posts` 不再为每个用户额外打一轮
- Cookie 改走临时文件 `--cookie-file`，不再出现在进程命令行
- stdout JSON 行缓冲，避免 `done` 被切成两半导致界面一直转圈
- `settings.json` 主进程合并写入，Cookie 键不会被并发保存互相覆盖
- 本地媒体协议：长期缓存 + ETag，路径限制在存档根
- 设置页可看/重登/清除平台和按用户 Cookie；浏览侧栏「更新」缺 Cookie 时会打开登录窗
- 浏览侧栏添加用户可填日期范围；缓存页注明已存档账号到浏览页更新
- Python unittest：`daterange` 与微博增量/置顶/原创判断

### v0.5.1（2026-08-27）

审查修复（全部完成）：

- 应用内登录：采集 Cookie 的 session 与登录窗口使用同一临时 partition，微博 / X / Instagram 登录后能真正写入 Cookie
- 浏览侧栏「更新」：按用户自己的平台拉缓存，不再因为运算符优先级把微博/Instagram 当成 X
- 批量队列：完成事件带 `type: 'batch-done'`，停止不再误启动下一任务；进度按主进程 `queued` 更新，侧栏显示当前用户名
- 单次缓存和批量缓存都会写 `_profile.json` 的 `lastUpdate`；Instagram 规范化后也会写 profile（名字 / 平台 / 头像）
- `Ctrl+1/2/3` 改为窗口内快捷键（`App.vue`），不再注册系统级 `globalShortcut`
- 缓存页开始缓存时同时写入 `per_user` Cookie；微博 Cookie 校验与界面一致，要求含 `SUB=`
- Instagram 浏览卡和离线 HTML 用 name + @handle + 时间，链接为 IG Blue
- `delete-archives` 对 Windows 盘符大小写做 `path.resolve` 比较，禁止删掉根目录本身
- 主进程帖子排序补上 Twitter snowflake 回退，与 `postTime.js` 一致

### v0.5.0（2026-08-27）

本次批量改进（14 项，全部完成）：

**界面 / 体验**
- Instagram 话题（`#tag`）和提及（`@handle`）高亮颜色从微博橙色 `#eb7350` 改为 Instagram 品牌蓝 `#0095f6`
- Instagram 离线 HTML 导出样式：渐变封面、IG Blue 链接、`#fafafa` 背景
- X 视频 fallback 文字（当视频无法播放时）在深色主题下从黑字改为 `color: inherit`，解决不可见问题
- 进度条显示真实 `current/total` 媒体数和 post 缓存进度
- 日志面板新增搜索框 + 类型过滤（全部/信息/错误/成功）
- 全局键盘快捷键：窗口聚焦时 `Ctrl+1/2/3` 切换下载/浏览/设置标签页（`App.vue` 监听 keydown，不再用系统级 globalShortcut）

**数据处理**
- gallery-dl 升级机制：优先用 `shutil.which('gallery-dl')`（用户可 `pip install -U` 升级），冻结版兜底，解决 frozen exe 内置版本无法升级的问题

**架构 / 质量**
- TypeScript 严格化：`DownloadEvent` 改为可辨识联合类型（`ProgressEvent | DoneResult | ErrorResult | StatusEvent`），删除旧宽松 interface，所有 IPC 回调类型化
- 批量多用户管理面板（`UserManager.vue`）：左侧边栏，显示已存档用户列表（平台图标/上次更新时间/更新+删除按钮），支持添加用户（platform selector + userId + cookie 即时验证）、批量勾选、批量更新/删除
- 按用户独立 Cookie 存储（`settings.cookies.per_user['platform:userId']`），不再只有全局 per-platform Cookie
- 批量队列串行执行（`processQueue` 递归），`lastUpdate` 写入 `_profile.json`
- `social-archiver://` 协议 Windows 路径解析修复：优先 `searchParams.get('path')`，保留驱动器前缀 `C:\`
- Python 版本门卫：打包构建时检查 `>=3.9`，不符合则明确报错
- Vitest 自动化测试：28 个测试覆盖 `session.js`（Cookie/用户键）、`postTime.js`（时间解析/排序稳定性）、`archiveHtml.js`（平台检测逻辑；现为 `detectPostPlatform`）

**安全修复（本次代码审查发现）**
- `delete-archives` 新增 `rootDir` 边界检查，防止路径穿越删除任意目录
- `stop-batch-download` 正确杀 `batchProcess`（批处理子进程）并重置所有状态
- `start-download` / 批量下载路径的 `mainWindow.webContents.send` 加 `?.` 防止窗口关闭时崩溃（`batch:done` 带 `type: 'batch-done'` 或 `batch-stopped`）
- `resolveBackendPath` 打包模式 exe 不存在时显式 throw，不再静默回退到无效 ASAR 路径

### v0.4.1（2026-08-22）

修复 Instagram Cookie 误存 twitter 键（`session.js` 按平台分键，浏览页「更新」不再拿 X 的 Cookie 跑 Instagram）；新增 Instagram 浅色皮肤（`skin.js` / `style.css` / `BrowseView` / `PostCard`）；删除 `HomeView` 未导入的 `path` 引用，恢复 `vue-tsc` / 打包。

---

## 打开开发版

```bat
cd /d D:\AI\work\social-archiver
npm.cmd run electron:dev
```

第一次先 `npm.cmd install` 和 `pip install -r backend\requirements.txt`。PowerShell 用 `npm.cmd`。

| 改了什么 | 要不要重启窗口 |
|----------|----------------|
| `electron-main.js` / `electron/**` / `preload.js` | 要 |
| Vue 前端 | 否（热更新） |
| `backend/*.py` | 否（下次点缓存就用新代码） |

打包：`npm.cmd run release:pack`（或 `release:check` 后 `electron:build`）。清单见 [RELEASE.md](RELEASE.md)。

产物（`<version>` = `package.json` 的 `version`）：

- `dist-electron/SocialArchiver-<version>-win.zip` — 解压即用
- `dist-electron/SocialArchiver-<version>-win.exe` — NSIS 安装包（可选）
- `dist-electron/win-unpacked/` — 同内容，本机可直接跑 `Social Archiver.exe`

PyInstaller onedir 输出到 `build-backend/backend/`，electron-builder 拷进 `resources/backend/`。**不要只拷贝 exe**，必须保留整个 `win-unpacked` 目录。对方不用装 Python。打包依赖：`pip install -r backend/requirements-build.txt`。若 `win-unpacked` 被占用（旧 exe 还在跑），先关掉再打包；也可 `--config.directories.output=%TEMP%\sa-electron-out` 打到临时目录再拷回。

运行测试：`npm.cmd test`

---

## 怎么工作

Electron 34 + Vue 3。微博自己请求 `m.weibo.cn`；X 和 Instagram 调 gallery-dl（优先系统已安装版，可升级；冻结版兜底）。主进程 spawn Python / 冻结 `backend.exe`，stdout 一行一条 JSON（UTF-8）。

```
electron-main.js      入口：窗口、调度器、CSP（electron/security.js）
electron/             IPC 分文件（ipc/*）、backend-spawn、protocol、settings-store、update-check、cookie-sources、cookie-validation
electron-archives.js  scan-archives / get-posts 纯逻辑（见 tests/fixtures/archive）
preload.js / json-lines.js / settings-merge.js
                  spawn backend；Cookie 走临时文件；stdout UTF-8 JSON Lines（带行缓冲）
backend/          cli.py（_force_utf8_stdio / --cookie-file）→ downloader.py → weibo.py | twitter.py | instagram.py
                  daterange.py  metadata.py  transcode.py  migrate_archives.py（旧平铺档迁平台子目录）
scripts/          build_backend.py（PyInstaller；含 Python >=3.9 门卫）  write_icon.py  electron-dev.ps1
src/views/        HomeView 缓存 · BrowseView 浏览（含 UserManager、BrowseCalendar） · SettingsView
src/components/   PostCard · LazyPost · UserManager · CookieInput · UserIdInput · LogPanel · BrowseCalendar
src/utils/        livePhoto.js  archiveHtml.js  postTime.js  session.js  userId.js  assetUrl.js  hashtags.js
tests/            Vitest（含 electron-archives、cookie-validation、update-check、hashtags、userId 等）+ Python unittest
```

存档在用户选的根目录 `{platform}/{user_id}/`（platform 为 weibo / twitter / instagram）：

- `_posts/{日期}/{id}.json`
- `{日期}/` 媒体文件
- `{日期}/媒体.ext.part`（仅在微博媒体下载中断且可续传时暂存；成功后自动改名）
- `_profile.json`（含 `lastUpdate` 时间戳）
- `_avatar.jpg` / `_avatar.png` / `_avatar.webp` / `_avatar.gif`（浏览会按这个顺序找）
- `index.html`（缓存成功会重写；浏览页也可再生成）
- X 另有 `.download-archive.sqlite`、`.twitter-cookies.txt`；平台目录另有共享 `.session-cookies.txt`（换用户沿用同一登录态）
- Instagram 另有 `.download-archive.sqlite`

批量缓存运行时的队列快照在应用数据目录 `%APPDATA%\social-archiver\pending-batch.json`。它只在有未完成批量任务时存在；正常完成或点「停止」会删除。恢复任务依赖已保存的本地 Cookie，并继续使用既有增量归档规则。

旧版平铺在 `{root}/{user_id}/` 的存档：浏览页仍可直接读；要归位到平台子目录，跑 `python backend/migrate_archives.py <根目录>`（预览），确认后加 `--apply` 实际移动。

Cookie：微博要 `m.weibo.cn` 的 `SUB`（不要 weibo.com 桌面站 Cookie）。X 要 `auth_token` + `ct0`（三处规则统一：`session.js`、`electron/cookie-rules.js`、`cookie_check.py`）。Instagram 要 `sessionid`。存储优先按 `platform:userId` 的 per_user 键（缓存页开始缓存会写 per_user），无则回退到 per_platform 全局键。`lastUpdate` 在单次缓存和批量缓存成功时都会写入 `_profile.json`。设置页 / 缓存页可从 Edge 一键导入并校验（`backend/browser_cookies.py` + gallery-dl `load_cookies`；读取前须完全退出 Edge）；**导入只写平台全局键，不覆盖已有 `per_user`**（v1.2.2）。进程内 Cookie 验证 TTL 30 分钟（`electron/cookie-validation.js`）：任务完成或 `checkCookie` 通过后，切换任务可跳过远端校验。Instagram 缓存前：已有可用 `sessionid` 则信任缓存，分区只在缓存为空或不可用时回填（`resolveInstagramJobCookie`）。已保存 Cookie 的输入区默认折叠为「已登录」。用户 ID 可粘贴主页 URL（`src/utils/userId.js`）。缓存页编辑 Cookie 约 0.6 秒后自动写入平台全局键（设置页可见）；任务结束微博 Cookie 由主进程写回，界面只更新输入框不再重复写盘。X 的 gallery-dl 会刷新 `ct0`：会话 Cookie 写在 `{root}/twitter/.session-cookies.txt`，同一 `auth_token` 换用户时沿用，并在任务结束写回设置 / 缓存页输入框，避免第二个账号带着过期 ct0 失败。

排序是时间戳，不是星期几字符串。微博实况图一格（静图 + 悬停）；X 和 Instagram 没有 LIVE 悬停。

本地文件协议：`social-archiver://asset/?path=`。`.mov` 按 `video/mp4`。≤100MB 的文件用整文件 Buffer 做 HTTP Range（`206`），`<video>` 才能拖进度。必须在 `app.whenReady` 之前 `registerSchemesAsPrivileged`（含 `standard` + `stream`）。播不了时 `shell.openPath`（preload：`openLocalPath`）。

不要用 `Readable.toWeb(fs.createReadStream)` 给 `<video>` 喂流，Range 会断。

---

## 微博

- API：`https://m.weibo.cn/api/container/getIndex`，`containerid=107603{uid}`。
- 翻页：`type=uid`、`value`、`since_id`。置顶博文不要当成「已到起始日期」。未设起始日期时，连续 5 条已缓存且媒体完整的原创（非置顶）就停翻，避免每次更新走满 200 页。打满 200 页要提示更早的没拉到。
- **翻页风控**：每页间隔约 8–13 秒；每连续 3 页额外暂停 45–75 秒；失败时任务内最多再等 3 分钟后保存断点，**请隔 30–60 分钟**再「深度回溯」。早年内容需多次续跑。参考 [RSSHub#20512](https://github.com/DIYgod/RSSHub/issues/20512)、[weibo-crawler#565](https://github.com/dataabc/weibo-crawler/issues/565)。
- **换日期范围**：不重置翻页断点，只按新范围过滤；不必重新登录。
- **登录检测**：任务开始最多 2 轮 `/api/config`；第 1 页不再重复 refresh（已有 `st` 时跳过），第 2 页起每页刷新 `st`。
- Cookie 回写：微博缓存成功结束（含翻页风控断点）时，若会话仍 `login: true`，主进程会把 `done.cookie` 写回 `settings.cookies.weibo` 与对应 `per_user`，避免翻页后 token 轮换但本地仍用旧 Cookie。
- 图片：不要把 API 的 `Host` / Cookie 带到 `wx*.sinaimg.cn`。Referer `https://weibo.com/`，走 `/large/`，wx1–4 重试。
- 头像：用户对象里的 `profile_image_url` / `avatar_hd`，写成 `_avatar.jpg`。
- 实况图下载必须走 play 接口，**不要** GET 裸的 `livephoto.us.sinaimg.cn`（403）。`video_url` 元数据保留 API 原始带签名地址（`Expires`/`ssig`），下载时再转 play URL；不要把查询参数剥掉。

```
GET https://video.weibo.com/media/play?livephoto=https%3A%2F%2Flivephoto.us.sinaimg.cn%2Fxxx.mov
Referer: https://weibo.com/
不要 Cookie，不要自定义 Host
```

返回 H.264 QuickTime。文件小于 64 字节或开头是 `{` `<` `[` 都是假文件，要重下（旧档里有 43 字节 JSON 冒充 mp4）。

相关代码：`backend/weibo.py`（`_livephoto_play_url`、`_media_file_ok`）、`src/utils/livePhoto.js`、`PostCard.vue`、`electron/protocol.js`（`social-archiver://`）。

悬停仍失败时：对应 `_N.mov` 是否有 `ftyp`；协议响应是不是 `video/mp4`；若是 HEVC（`hvc1`），窗口里解不了，应出现「打开实况」。

---

## X

- `backend/twitter.py` → 冻结后的 `backend.exe --run-gallery-dl` 或开发态 `python -m gallery_dl` → `https://x.com/{handle}/tweets`。纯数字 ID 用 `https://x.com/id:{id}/tweets`。不要用 `/media`。
- 只要原创 + 纯文字帖。`retweets/quoted/replies` 关。`size: orig`，不要视频封面、卡片、文章附件，丢掉 m3u8。视频取 `video_info.variants` 里 bitrate 最高的 mp4。
- 日期用 `date-after` / `date-before`。边界在 `backend/daterange.py`。
- 没有微博那种实况图。GIF 也是 mp4，浏览里用控件播。
- 头像：推文拉完再跑一次 `https://x.com/{handle}/photo`（数字 ID 用 `id:{id}/photo`），文件名 `_avatar.{extension}`，写到用户目录根。失败或超时（45 秒）不打断整次缓存，浏览页先显示首字母。
- gallery-dl 优先系统已安装版（`shutil.which('gallery-dl')`），用户可 `pip install -U gallery-dl` 升级；冻结版兜底。
- **限流**：`sleep` 1.5–4s、`sleep-request` 6–12s、`sleep-429` 60s、`ratelimit: wait:300`；下载线程上限 2。参考 [gallery-dl#5775](https://github.com/mikf/gallery-dl/issues/5775)。429 由 gallery-dl 自动退避，不要手动杀进程。
- **换用户**：同一 `auth_token` 沿用 `{root}/twitter/.session-cookies.txt`（gallery-dl 会刷新 `ct0`）；不要每次用界面里的旧 Cookie 覆盖。任务结束把 Cookie 写回设置。

相关代码：`backend/twitter.py`（`_fetch_twitter_avatar`、`_normalize_twitter_archive`）、`backend/metadata.py`（`existing_avatar`）、`electron-archives.js`（`findAvatar`）。

---

## Instagram

- 通过 gallery-dl 下载，优先系统已安装版（`shutil.which('gallery-dl')`），可 `pip install -U gallery-dl` 升级。只认 `sessionid` Cookie，不支持用户名密码登录。
- 用户 ID 可以是用户名（如 `username`）或数字 ID（如 `id:12345`）。
- 下载图片、视频、轮播（carousel），存到 `{user_id}/{YYYY-MM-DD}/`。
- 日期范围通过 `date-after` / `date-before` 控制。增量靠 `.download-archive.sqlite` 跳过已下媒体。
- 头像：单独跑一次 gallery-dl avatar extractor，文件名 `_avatar.{extension}`，超时 45 秒不打断主流程。
- 私密账号：需登录用户已关注目标，否则会报错。
- 请求间隔：gallery-dl 内置 6-12 秒随机延迟。
- Cookie 存储按 `settings.cookies.per_user['instagram:userId']`（v0.5.0 新增），无则回退到 `settings.cookies.instagram`。缓存 IG 时：已有可用 `sessionid` 则用缓存；仅当缓存为空或不可用时才从应用内登录分区回填（`resolveInstagramJobCookie`，v1.2.2）。
- 阅读主题统一为浅色/深色（顶栏切换，默认浅色）；帖子布局仍按平台区分。话题/提及用 `--sa-entity` 高亮。Instagram 浏览卡和离线 HTML 为桌面式分栏（左媒体 / 右配文，轮播含箭头与圆点）。

相关代码：`backend/instagram.py`（`download_instagram_media`、`_normalize_instagram_archive`）、`backend/metadata.py`（`download_avatar`）、`electron/ipc/login.js`（`instagram-login`）。

---

## macOS / Linux 冒烟（评估，非正式产物）

当前 **electron:build 仅配置 Windows**（zip + NSIS）。跨平台需在本机额外试：

```bash
# 后端（任意 OS）
pip install -r backend/requirements.txt -r backend/requirements-build.txt
python scripts/build_backend.py   # 产出 build-backend/backend/

# 前端
npm run build

# macOS（需本机 Xcode CLI）
npx electron-builder --mac --dir

# Linux
npx electron-builder --linux --dir
```

**已知阻塞 / 注意：**

- PyInstaller 需在目标 OS 上分别打包 `backend`；不能把 Windows 的 `backend.exe` 直接给 macOS/Linux
- `gallery-dl` 仍建议系统安装，冻结版作兜底
- 未在 CI 持续验证；Phase 5 结论：**可技术可行，正式支持需单独发版与图标/签名策略**
- Windows 仍是唯一承诺的分发平台

---

## 已知问题

- 安装包除 zip 外另有 NSIS `.exe`（`npm run electron:build`）。解压版与安装版均**未代码签名**，可能 SmartScreen 拦截；见 README「Windows SmartScreen」。
- 打包版缓存日志曾按 GBK 输出导致界面乱码；已在 `cli.py` + `electron/backend-spawn.js` 固定 UTF-8。若仍乱码，确认用的是新 zip / 新 `win-unpacked`，不是旧目录里单独拷出来的 exe。
- 微博应用内登录部分机器会闪退；改在系统浏览器打开 `m.weibo.cn` 后粘贴 Cookie。日志：`%APPDATA%\social-archiver\weibo-login-debug.log`。
- 启动关 GPU / WebGL，避免旧崩溃。因此窗口里解不了 HEVC；这是预期，不是下载失败。
- 超过 100MB 的媒体走流式 Range，Chromium 里拖进度可能失败；窗口播不了时用卡片上的系统播放器打开。
- Instagram 需 `sessionid` Cookie，且只支持公开账号或登录用户已关注的私密账号。sessionid 过期后需重新登录。
- Instagram 请求间隔为 6-12 秒，大账号缓存较慢，属正常限流保护。

未做（用户没点名就别做）：去 Element、大文件 `_preview.mp4` 降码率副本、跨帖媒体 SHA256 去重。

已实现但可选：**ffmpeg** 转 H.264 / poster（`backend/transcode.py`，设置页检测 PATH，默认关）。

---

## 配置

`%APPDATA%\social-archiver\settings.json`：输出目录、并发、命名模板、上次平台。Cookie 只留本机。

```json
{
  "output_dir": "D:/archives",
  "concurrent": 3,
  "naming_template": "{post_id}_{index}",
  "proxy_url": "",
  "update_manifest_url": "",
  "scheduler": { "enabled": false, "run_at": "03:00" },
  "ffmpeg": { "enabled": false },
  "notifications": { "enabled": true },
  "batch_retry": { "enabled": true, "max_attempts": 3 },
  "cookies": {
    "twitter": "auth_token=...; ct0=...",
    "weibo": "SUB=...",
    "instagram": "sessionid=...",
    "per_user": {
      "twitter:amd": "auth_token=...; ct0=...",
      "instagram:username": "sessionid=..."
    }
  },
  "user_schedules": {},
  "user_last_scheduled": {}
}
```

便携模式：exe 同目录放 `portable.flag`，配置写入同目录 `data/settings.json`（见 v0.7.0 版本历史）。

---

## 测试

`npm.cmd test`（Vitest **87** 项 + Python unittest **79** 项）。主要覆盖：

- `session.test.ts`：`hasUsableCookie`（微博 SUB / X auth_token+ct0 / IG sessionid）
- `userId.test.ts`：IG / X 用户名规范化、主页 URL 抽出 ID、非法字符检测
- `postTime.test.ts`：时间戳解析、`sortPosts` 排序稳定性/边界
- `archiveHtml.test.ts`：`detectPostPlatform`、Instagram 分栏离线 HTML、轮播 slide
- `hashtags.test.ts`：话题提取与筛选
- `assetUrl.test.ts` / `jsonLines.test.ts`：本地资源 URL、stdout 行缓冲、settings 合并
- `tests/electron-archives.test.js`：fixture 存档 scan → get-posts 链路
- `tests/electron-cookie-sources.test.js`：Edge 导入只写平台全局键，不覆盖 `per_user`
- `tests/electron-cookie-validation.test.js`：进程内 Cookie 验证 TTL
- `tests/update-check.test.js`：更新清单解析
- `tests/test_daterange.py` / `tests/test_weibo.py` / `tests/test_gallery_dl_runner.py` / `tests/test_browser_cookies.py` 等

新增前端测试：`src/**/*.test.ts` 或 `tests/**/*.test.js`。Python：`tests/test_*.py`。`test:watch` 可监听 Vitest。发版前建议 `npm.cmd run release:check` 或 `npm.cmd run build`。
