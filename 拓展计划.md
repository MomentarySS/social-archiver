# Social Archiver 拓展计划

> 记录于 2026-08-29，末次梳理 2026-08-31（v1.2.0）。基于 v0.5.4 现状与产品边界（见 [PRODUCT.md](PRODUCT.md)）整理。  
> 原则：磁盘目录仍是存档真相；不引入云同步、多用户服务；引用/转发不得冒充原创。

> **§1–§7 说明**：以下为早期愿景与优先级讨论；**是否已落地以 [§8 分阶段实施计划](#8-分阶段实施计划) 为准**。表中 P0/P1/P2 为当初排期，不表示「仍待开发」。

---

## 目录

1. [内容维度扩展（三平台深挖）](#1-内容维度扩展三平台深挖)
2. [阅读与检索体验](#2-阅读与检索体验)
3. [自动化与运维](#3-自动化与运维)
4. [媒体处理](#4-媒体处理)
5. [导出与外部集成](#5-导出与外部集成)
6. [分发：安装器、签名、便携模式](#6-分发安装器签名便携模式)
7. [架构演进](#7-架构演进)
8. [分阶段实施计划](#8-分阶段实施计划)

---

## 1. 内容维度扩展（三平台深挖）

当前只收**原创帖**。以下均在「可选开关 + 明确标注来源」前提下扩展，不违背 PRODUCT 原则。

### 1.1 跨平台通用

| 方向 | 说明 | 优先级 |
|------|------|--------|
| 引用/转发存档 | 保留上下文，帖子上标注「引用自 @xxx」或「转发自」 | P2 |
| X 回复时间线 | 用户自己发的回复帖（独立帖文，`kind: reply`）；`/with_replies` + `replies: true` | **P0** |
| 微博评论区配图 | 需评论 API，覆盖不全；本阶段不做 | 暂缓 |
| 置顶帖 | X `pinned: true`；微博已识别置顶，可纳入浏览排序 | P2 |
| 纯文字帖强化 | 已支持下载；加强导出与搜索权重 | P1 |

### 1.2 微博

| 方向 | 说明 | 优先级 |
|------|------|--------|
| **深度回溯模式** | 突破「连续 5 条已缓存即停」；用户显式开启后继续翻页 | **P0** |
| **分页续拉 / 断点** | 记录 `since_id` 或最后处理页码到 `_profile.json`，下次从断点继续 | **P0** |
| 200 页上限提示优化 | 打满 200 页时明确告知「更早内容未拉取」，引导开深度模式 | P1 |
| 实况图修复工具 | 检测 43 字节假 mp4、无 `ftyp` 文件，一键重下 | P1 |

**现状（`backend/weibo.py`）：**

- `MAX_PAGES = 200`
- `EXISTING_STREAK_STOP = 5`（连续 5 条已缓存且媒体完整的原创非置顶 → 停止）
- 日常增量友好，但老账号首次全量或补历史时容易「误以为已完成」

**深度回溯模式设计草案：**

```
设置项 / 缓存页勾选：「深度回溯（忽略已缓存停翻，直至日期范围或 200 页上限）」
_profile.json 新增：
  lastSinceId: string      # 上次翻页游标
  lastPage: number
  deepBacktrack: boolean   # 上次是否深度模式
  fetchStatus: "complete" | "partial" | "page_limit"
```

### 1.3 X（Twitter）

| 方向 | 说明 | 优先级 |
|------|------|--------|
| 书签（Bookmarks） | gallery-dl 已支持；独立目录或子类型 `_posts` | P2 |
| 点赞（Likes） | 同上 | P3 |
| 引用帖（Quotes） | `quoted: true`；须与原创分区展示 | P2 |
| 媒体时间线 | `/media` 端点；与推文时间线二选一或并存 | P3 |

### 1.4 Instagram

| 方向 | 说明 | 优先级 |
|------|------|--------|
| Stories | 时效内容；缓存时提示 24h 风险 | P2 |
| Reels | 与帖子合并或分 tab | P2 |
| Carousel 元数据补全 | 轮播序号、caption 与多图关联加强 | P1 |

---

## 2. 阅读与检索体验

v1.2.0 起浏览页已具备搜索、日期筛选、画廊、月历、话题筛选等（见 Phase 1–2、Phase 6–7）。下列为原始功能清单，落地状态见 §8。

### 2.1 功能清单

| 功能 | 实现思路 | 优先级 | 状态 |
|------|----------|--------|------|
| **全文搜索** | 扫描 `_posts/*.json` 建索引 | **P0** | ✅ `_index/search-cache.json` |
| 关键词高亮 | 搜索结果命中片段标黄 | P1 | ✅ Phase 6 |
| 按日期筛选 | 日期范围 picker，过滤 `created_at` / 目录名 | P1 | ✅ Phase 6 |
| 按标签/话题筛选 | `#话题` 正则或索引字段 | P2 | ✅ v1.2.0 |
| 跨用户统一时间线 | 合并多用户 `_posts`，全局时间排序 | P2 | ✅ Phase 4 |
| 纯媒体画廊 | 从 `pics` 聚合，网格视图，点击进帖 | P1 | ✅ Phase 2 |
| 日历导航 | 按 `_posts/{date}/` 跳转，侧边栏月历 | P2 | ✅ v1.2.0 |
| 帖子深链接 | URL hash `#post-{id}`，离线 HTML 同步支持 | P1 | ✅ Phase 1 |
| 浏览统计 | 总帖数、媒体数、最早/最晚日期、磁盘占用 | P1 | ✅ Phase 2 |

### 2.2 技术要点（已实现）

- 索引文件：`{存档根}/_index/search-cache.json`（非 SQLite；早期草案曾写 `search.sqlite`）
- 首次打开目录或检测到 `lastUpdate` 变化时后台重建索引
- 与 `LazyPost` 兼容：搜索结果是 ID 列表，再按需挂载卡片

---

## 3. 自动化与运维

应用内 scheduler、批量队列、校验、更新历史、桌面通知与批量失败退避（v1.2.0）已落地；下列为原始清单。未做：**导出 Windows 计划任务 .bat**。

### 3.1 功能清单

| 功能 | 说明 | 优先级 | 状态 |
|------|------|--------|------|
| **Cookie 过期检测** | 缓存前轻量 API 探测；设置页标红 + Toast | **P0** | ✅ Phase 1 |
| **定时自动更新** | 应用内 scheduler 或导出「计划任务脚本」 | **P0** | ✅ 应用内 scheduler；❌ 计划任务 .bat |
| 每用户更新策略 | 手动 / 每日 / 每周；写入 `settings.json` | P1 | ✅ Phase 2 |
| 失败重试 | 单用户失败后指数退避重试，记入日志 | P1 | ✅ v1.2.0 `batch_retry` |
| **完整性校验** | JSON 引用媒体是否存在、大小阈值（如 &lt;64B） | **P0** | ✅ Phase 1 |
| 修复向导 | 列出损坏项 → 选择性重下 | P1 | ✅ Phase 6 |
| 无头 CLI 批处理 | `backend.exe --batch-config users.json` | P1 | ✅ Phase 2 |
| 更新历史 | `_profile.json` 或根目录 `update-log.jsonl` | P2 | ✅ Phase 6 |
| 桌面通知 | 批量完成 / 失败 / Cookie 过期 | P2 | ✅ Phase 6 |

### 3.2 定时更新设计草案

```json
// settings.json 扩展
{
  "scheduler": {
    "enabled": false,
    "interval_hours": 24,
    "run_at": "03:00",
    "only_users": [] 
  }
}
```

Windows 可选：生成 `.bat` + 任务计划程序 XML，供不想常开应用的用户。

---

## 4. 媒体处理

Phase 3 已实现可选 ffmpeg 转码（HEVC→H.264）与视频 poster；下列为完整媒体路线图，**状态见「状态」列**。

### 4.1 问题与方案

| 问题 | 方案 | 优先级 | 状态 |
|------|------|--------|------|
| 微博实况 HEVC 窗口内无法播放 | 可选 ffmpeg → H.264；或仅生成预览 MP4 | P1 | ✅ 转码；❌ 仅 preview 副本 |
| 大文件（&gt;100MB）Range 拖动差 | 流式优化；或转码降码率副本 `_preview.mp4` | P2 | ⏳ 未做 |
| 视频无封面 | ffmpeg 抽第一帧 → `_poster.jpg` | P2 | ✅ Phase 3.5 |
| 损坏文件检测 | 已有微博假 mp4 逻辑；统一到校验工具 | P1 | ✅ Phase 1 + 6 修复向导 |
| 跨帖重复媒体 | 可选按 SHA256 硬链接或跳过（高级） | P3 | ⏳ Phase 7 暂缓 |

### 4.2 原则

- ffmpeg **默认关闭**，设置页检测系统 PATH 后可选启用
- 打包版不捆绑 ffmpeg（体积与许可）；文档说明安装方式
- 转码产物命名约定：`{原名}_h264.mp4`，原文件保留

---

## 5. 导出与外部集成

离线 `index.html` 已有；扩展格式让存档进入笔记与备份工作流。

### 5.1 格式清单

| 格式 | 输出结构 | 优先级 |
|------|----------|--------|
| **Markdown（单用户）** | 每帖 `{date}/{id}.md` + 媒体相对路径 | **P0** |
| **Obsidian 库** | 上述 + `[[双向链接]]` 可选 front matter | P1 |
| JSON 全量导出 | 单文件 `export-{user}-{date}.json` | P1 |
| RSS | 本地 feed.xml，供阅读器订阅自己的存档 | P2 |
| PDF 按年月 | 电子书式分页，适合印刷级备份 | P3 |
| 多用户静态站 | 根目录 `site/`，类 GitHub Pages（仍本机生成） | P3 |

### 5.2 Markdown 示例

```markdown
---
id: 5123456789
date: 2024-06-15
platform: weibo
url: https://weibo.com/...
---

正文内容…

![image](./2024-06-15/5123456789_1.jpg)
```

---

## 6. 分发：安装器、签名、便携模式

| 方向 | 现状 | 目标 | 优先级 |
|------|------|------|--------|
| 安装器 | zip 解压 | NSIS 或 MSI，开始菜单快捷方式 | P2 |
| 代码签名 | 无，SmartScreen 拦截 | Authenticode 签名 + 文档说明自签替代 | P3 |
| 便携模式 | 配置在 `%APPDATA%\social-archiver\` | 检测 exe 旁 `portable.flag` → 配置跟目录走 | P1 |
| macOS / Linux | 仅 Windows | Electron 跨平台构建（后续大版本） | P3 |

便携模式路径约定：

```
Social Archiver.exe
portable.flag          # 存在则启用
settings.json          # 与 exe 同目录或 ./data/
archives/              # 可选默认下载根
```

---

## 7. 架构演进

平台增多后，减少 `PLATFORMS` / `choices` / 三处 UI 硬编码。

### 7.1 目标形态

```
PlatformAdapter（每平台一个模块）
├── id, label, cookieSchema
├── download(userId, opts) → AsyncIterator<Event>
├── normalizeArchive(userDir) → Post[]
├── browse: { postComponent, htmlTemplate }
└── optional: loginHandler
```

### 7.2 演进步骤

| 阶段 | 内容 | 优先级 |
|------|------|--------|
| A | 抽取 `gallery_dl_runner.py`，合并 twitter.py / instagram.py 重复逻辑 | P1 |
| B | `src/platforms/registry.ts` 注册表驱动 `PlatformSelector` | P2 |
| C | `PostCard` 拆为 `WeiboPost` / `XPost` / `IgPost`，由注册表映射 | P2 |
| D | 可选插件目录 `plugins/*.py`（高级用户） | P3 |

### 7.3 不宜过早做的

- 完整插件 SDK 与沙箱
- 微服务化 backend
- 在架构未稳定前接入 &gt;3 个新平台

---

## 8. 分阶段实施计划

### 总览

| 阶段 | 主题 | 目标版本 | 核心交付 |
|------|------|----------|----------|
| **Phase 1** | 可用性底座 | v0.6.x | 搜索、微博深度回溯、校验、Cookie 检测 |
| **Phase 2** | 效率与导出 | v0.7.x | 定时更新、Markdown 导出、媒体画廊、便携模式 |
| **Phase 3** | 内容与媒体 | v0.8.x | X 回复时间线、ffmpeg 转码、可选引用/置顶 |
| **Phase 4** | 平台与架构 | v0.9.x | PlatformAdapter、gallery-dl 抽象、安装器 |
| **Phase 5** | 生态与分发 | v1.0 | Obsidian/RSS、代码签名、跨平台评估 |
| **Phase 6** | 阅读与运维深化 | v1.1 | 日期筛选、搜索高亮、JSON 导出、修复向导 |
| **Phase M** | 本地维护纪律 | v1.1.1 | 测试全绿、版本真源、`release:pack`、CHANGELOG / RELEASE |
| **Phase 7** | 阅读延伸 | v1.2.0 | 月历、话题筛选、批量退避、检查更新（部分暂缓见下表） |

---

### Phase 1 — 可用性底座（v0.6.x）✅ 已完成（2026-08-29）

| # | 任务 | 状态 |
|---|------|------|
| 1.1 | 微博「深度回溯」开关 + `_profile.json` 断点字段 | ✅ |
| 1.2 | 200 页打满 `fetchStatus: page_limit` | ✅ |
| 1.3 | 浏览页全文搜索（索引缓存 `_index/search-cache.json`） | ✅ |
| 1.4 | 帖子深链接 `#post-{id}` | ✅ |
| 1.5 | 完整性校验 `backend/verify.py` + 设置页入口 | ✅ |
| 1.6 | Cookie 过期预检 `backend/cookie_check.py` | ✅ |

---

### Phase 2 — 效率与导出（v0.7.x）✅ 已完成（2026-08-29）

| # | 任务 | 状态 |
|---|------|------|
| 2.1 | 定时批量更新（应用内） | ✅ |
| 2.2 | 每用户更新频率配置 | ✅ |
| 2.3 | Markdown 导出 | ✅ |
| 2.4 | 媒体画廊视图 | ✅ |
| 2.5 | 浏览统计面板 | ✅ |
| 2.6 | 便携模式 | ✅ |
| 2.7 | 无头 CLI 批处理 | ✅ |

---

### Phase 3 — 内容与媒体（v0.8.x）✅ 已完成（2026-08-29）

**目标：** 可选扩展内容类型；改善视频播放体验。

**推荐实施顺序：** 3.0 → 3.4 → 3.1 → 3.2 → 3.6 → 3.3 → 3.5

| # | 任务 | 涉及文件（预估） | 验收标准 |
|---|------|------------------|----------|
| **3.0** | **X 回复时间线（可选）** | `twitter.py`, `PostCard.vue`, `HomeView.vue` | ✅ |
| 3.1 | X / 微博「引用帖」可选开关 | `twitter.py`, `weibo.py`, `PostCard.vue` | ✅ |
| 3.2 | 置顶帖纳入（X `pinned`） | `twitter.py`, 排序逻辑 | ✅ |
| 3.3 | IG Reels / Stories（可选） | `instagram.py` | ✅ |
| 3.4 | ffmpeg 可选转码（HEVC→H.264） | 新 `backend/transcode.py`, 设置页 | ✅ |
| 3.5 | 视频 poster 生成 | `transcode.py` | ✅ |
| 3.6 | Carousel 元数据补全 | `instagram.py`, `metadata.py` | ✅ |

**明确不做（本阶段）：** 微博评论区配图（API 散、覆盖不全）

**依赖：** Phase 1 校验；用户自行安装 ffmpeg  
**估时：** 3–4 周

---

### Phase 4 — 平台与架构（v0.9.x）✅ 已完成（2026-08-29）

**目标：** 降低新平台接入成本；改善 Windows 安装体验。

| # | 任务 | 状态 |
|---|------|------|
| 4.1 | `gallery_dl_runner.py` 抽象 | ✅ |
| 4.2 | `PlatformAdapter` 注册表（Python + TS） | ✅ |
| 4.3 | `PostCard` 按平台拆分 | ✅ |
| 4.4 | NSIS 安装器 | ✅ |
| 4.5 | 跨用户统一时间线（只读） | ✅ |

**依赖：** Phase 1 搜索索引  
**估时：** 3–4 周

---

### Phase 5 — 生态与分发（v1.0）✅ 已完成（2026-08-29）

**目标：** 对外可称为「1.0」的完整个人归档工具。

| # | 任务 | 状态 |
|---|------|------|
| 5.1 | Obsidian 导出优化 | ✅ front matter、标签、按年文件夹 `export-md/{year}/{date}/` |
| 5.2 | 本地 RSS | ✅ `feed.xml` + 浏览页「导出 RSS」 |
| 5.3 | X 书签 / 点赞（可选模式） | ✅ `{user}--bookmarks` / `{user}--likes` 独立目录 |
| 5.4 | 代码签名 | ✅ SmartScreen 说明（README / 交接文档）；无证书不捆绑 |
| 5.5 | macOS / Linux 构建评估 | ✅ 交接文档记录冒烟步骤与已知阻塞 |
| 5.6 | 回复串（若仍有需求） | 暂缓（视反馈） |

**估时：** 4+ 周，可与 4 并行部分任务

---

### Phase 6 — 阅读与运维深化（v1.1）✅ 已完成（2026-08-29）

**目标：** v1.0 之后提升「找得到、修得好、导得出」的日常体验。

| # | 任务 | 状态 |
|---|------|------|
| 6.1 | 浏览页日期范围筛选 | ✅ |
| 6.2 | 搜索命中关键词高亮 | ✅ |
| 6.3 | JSON 全量导出 | ✅ |
| 6.4 | 校验修复向导 | ✅ 查看帖子 / 更新用户 |
| 6.5 | 微博实况假 mp4 修复 | ✅ 设置页一键删除损坏文件 |
| 6.6 | 更新历史 + 桌面通知 | ✅ `update-log.jsonl` + 可选系统通知 |

---

### Phase M — 本地维护纪律（v1.1.1）✅ 已完成（2026-08-31）

路线 2：**本地 git + 偶尔发 zip**，不上 GitHub Actions。发版清单见 [RELEASE.md](RELEASE.md)。

| # | 任务 | 状态 |
|---|------|------|
| M.1 | `normalize_concurrent(0)` 等失败测试修复；`npm test` 全绿 | ✅ |
| M.2 | 版本号真源统一到 `package.json`（1.1.1） | ✅ |
| M.3 | `release:check` / `release:pack` 本地脚本 | ✅ |
| M.4 | [CHANGELOG.md](CHANGELOG.md) + [RELEASE.md](RELEASE.md) | ✅ |
| M.5 | 发版演练：`SocialArchiver-1.1.1-win.zip` + 本地 `v1.1.1` tag | ✅ |

**本阶段刻意不做：** GitHub Actions、`electron-main.js` 拆分、代码签名、自动更新。

---

### 长期维护 backlog（务实顺序）

v1.1.1 之后的**结构/流程投资**与 **Phase 7 产品功能**分开排。下列顺序来自维护讨论共识；**不是固定 Phase 编号**，按触发条件「顺手做」即可。

```mermaid
flowchart TD
    done1["✅ 修失败测试 + 版本同步"] --> done2["✅ CHANGELOG + Release 流程"]
    done2 --> done3["✅ electron 主进程拆分"]
    done3 --> done4["✅ fixture 存档 + IPC 集成测试"]
    done4 --> done5a["✅ 检查更新 + 签名文档"]
    done5a --> done5b["✅ Phase 7（月历 / 话题 / 退避）"]
    done5b --> branch{要对外长期分发?}
    branch -->|是| dist["Authenticode 证书签名打包"]
    branch -->|否| phase7["Phase 7 产品小步迭代"]
    ci["GitHub Actions CI"] -.->|上 GitHub 时| done2
```

| 顺序 | 项 | 状态 | 何时做 / 说明 |
|------|-----|------|----------------|
| 1 | 修失败测试 + 版本号真源 | ✅ | Phase M（v1.1.1） |
| 2 | CHANGELOG + 本地发版流程 | ✅ | `release:pack`、 [RELEASE.md](RELEASE.md) |
| 3 | `electron-main.js` 模块化拆分 | ✅ v1.2.0 | `electron/` + `electron-archives.js`；IPC 分文件注册（未单独发 v1.1.2） |
| 4 | `tests/fixtures/` 脱敏存档 + IPC 集成测 | ✅ v1.2.0 | `tests/fixtures/archive/` + `tests/electron-archives.test.js` |
| 5a | 代码签名 + 自动更新 | ✅ v1.2.0 | 设置页检查更新 + [SIGNING.md](SIGNING.md)；无证书时不签名 |
| 5b | [Phase 7 候选](#phase-7-候选未排期) 产品功能 | ✅ 部分 | v1.2.0：月历、话题、批量退避；暂缓 X /media、去重、PDF |
| 6 | v1.2.0 本地发版 | ✅ | `release:pack` → `SocialArchiver-1.2.0-win.zip` + tag `v1.2.0` |
| — | GitHub Actions CI | 可选 | **上 GitHub 时**；把 `release:check` 搬进 workflow 即可 |

**若只愿意做一件事：** 保持每次发 zip 前跑 `npm.cmd run release:pack`（已落地）。  
**下一步（维护 backlog）：** v1.2.0 已发本地 zip；有 Authenticode 证书时按 [SIGNING.md](SIGNING.md) 签名打包，或从 Phase 7 暂缓项（X `/media`、去重、PDF）中择项迭代。  
**平台 API 变了：** 先修缓存链路，再考虑 Phase 7 新功能。

**不必为「长期维护」去做：** 换框架（Tauri/React）、重型 UI 库、微服务化、追求 90% 覆盖率。

---

### 暂缓功能（用户确认，2026-08-29）

以下两项**暂不实施**，不列入 Phase 7 默认排期；若日后有强需求再单独立项。

| 功能 | 原计划位置 | 暂缓原因 | 已有替代 |
|------|------------|----------|----------|
| **微博评论区配图** | §1.1 | 评论 API 散、覆盖不全，维护成本高 | 原创帖媒体已完整缓存 |
| **X 回复串** | Phase 5.6 | 需补拉父帖 + 串视图 UI，复杂度高 | **X 回复时间线**（Phase 3.0 ✅）：用户自己发的回复帖独立存档 |

**区分：**「回复时间线」= 用户发的回复帖；「回复串」= 一条推文下整棵对话树。后者未做。

---

### Phase 7 — 阅读延伸（v1.2.0）✅ 部分完成（2026-08-31）

| # | 任务 | 状态 |
|---|------|------|
| 7.1 | 日历月历导航 | ✅ |
| 7.2 | 话题 / 标签筛选 | ✅ |
| 7.3 | 批量失败指数退避 | ✅ |
| 7.4 | X `/media` 时间线 | ⏳ 暂缓 |
| 7.5 | 跨帖重复媒体去重 | ⏳ 暂缓 |
| 7.6 | PDF 按年月导出 | ⏳ P3 |

---

### Phase 7 候选（未排期）

v1.1 之后若继续迭代，可从 backlog 中择 4–6 项组成 Phase 7（主题待定，如「阅读延伸」）：

| 候选 | 来源 | 说明 |
|------|------|------|
| 日历月历导航 | §2.1 | 按 `_posts/{date}/` 侧边栏跳转 |
| 话题 / 标签筛选 | §2.1 | `#话题` tab 或索引字段 |
| X `/media` 时间线 | §1.3 | 与推文时间线并存或二选一 |
| 批量失败指数退避 | §3.1 | 单用户失败后自动重试 |
| 跨帖重复媒体去重 | §4.1 | SHA256 硬链接（高级） |
| PDF 按年月导出 | §5.1 | P3，依赖排版方案 |

---

## 风险与约束

| 风险 | 缓解 |
|------|------|
| 平台 API / gallery-dl 变更 | 保持「系统 gallery-dl 优先」策略；锁定最低版本并 CI 测 |
| 深度回溯触发风控 | 加大 `REQUEST_SLEEP`；UI 提示勿频繁全量 |
| ffmpeg 许可与体积 | 不捆绑；可选依赖 |
| 范围膨胀 | 每阶段最多 6–7 项；P3 不进当前阶段 |
| 与 PRODUCT 冲突 | 新内容类型必须默认关闭 + 来源标注 |

---

## 文档同步

每个阶段发版时更新：

- [README.md](README.md) — 用户可见功能
- [CHANGELOG.md](CHANGELOG.md) — 用户向版本变更
- [RELEASE.md](RELEASE.md) — 发 zip 清单（维护发版时）
- [交接文档.md](交接文档.md) — 实现细节与踩坑
- [PRODUCT.md](PRODUCT.md) — 仅当产品边界变化时
- 本文件 — 勾选已完成任务，注明版本号

---

## 快速参考：优先级定义

| 级别 | 含义 |
|------|------|
| **P0** | 当前阶段必做；不做会明显阻碍核心场景 |
| **P1** | 当前或下一阶段；显著提升体验 |
| **P2** | 有价值但可延后 |
| **P3** | 锦上添花或依赖外部条件（证书、新平台） |
